---
name: naive-cream-illustration
description: >
  拙趣奶油插画风格图片生成器。用户提供任意主题（一句话、一个词、一个场景都行），
  按"纸白大留白 × 低饱和奶油系配色 × 稚拙手绘 × 拙拙感手写排版"的固定视觉体系生成 1:1 方形插画。
  Use when 用户提到"我喜欢的风格"、"那套配色"、"奶油风"、"拙趣风"、"治愈插画"、
  "小红书那种感觉"、"喜茶风"、或要求按之前定下的插画风格出图；也用于后续任何想保持
  该风格统一输出的图片请求。不复制任何原图，只应用风格规则。
metadata:
  category: design
---

# 拙趣奶油插画风格生成

用户收藏的小红书参考图（喜茶"拙趣"品牌风 × yomsweethome 治愈系插画）背后是同一套视觉语言。
本 skill 把该语言固化为可复用规则，任何主题输入都按此产出，风格稳定统一。

## 触发与输入

- 用户给出任意主题（中文/英文皆可），例如"秋天的第一杯奶茶"、"窗台晒太阳的猫"、"周末市集"。
- 可选附加要求：是否带文字、文字内容、希望偏哪个色系（橄榄绿 / 陶土红 / 雾霾蓝 / 奶油黄 / 薄荷绿）。

## 执行步骤

1. 先读 `references/style-guide.md`，把风格规范装入上下文（配色、质感、排版、构图、禁用项）。
2. 与用户确认主题（必要时）与两个选项：**要不要文字**、**主点缀色倾向**。用户没提就按规范默认执行：不带文字，色系从规范默认清单里挑最贴合主题的。
3. 按 style-guide 中的"提示词模板"组装生图提示词：主题 + 风格块 + 文字要求（如有）。
4. 调用图片生成工具：
   - 比例固定 **1:1（方形）**，尺寸 1K 或 2K；
   - filename 用用户语言的主题短名（如 `autumn-milk-tea` / `秋日奶茶`）；
   - 图片保存到当前项目的 workspace 目录（samples/ 下），无项目则放 outputs。
5. 生成后检查：留白是否足够、配色是否低饱和、笔触是否稚拙。若中文文字被模型写花（常见），
   若为小字说明可接受"歪歪扭扭"（本来就是拙拙感），若花到不可读则重生成一次；仍不行就放弃文字、只保留插画，并告知用户。

## 风格要点（速记版，完整规范见 style-guide.md）

- 纸白底 + 80% 以上留白，主体小而居中/略偏侧，四周透气。
- 低饱和"奶油系"：一次只挑 1~2 个点缀色，深色（炭黑/深橄榄）仅用于文字与轮廓。
- 拙趣手绘：儿童简笔画式的稚拙造型、铅笔草稿线、鼠标画般的不熟练线条、纸纹噪点、平涂无写实光影。
- 排版（带文字时）：拙拙感手写中文，圆润略歪、字距宽松、放松不对齐；一个标题大字 + 少量小字。
- 氛围：松弛、治愈、日常、不费力——"别那么累，放轻松"。

## 铁律

- **不复制原图**：不出现喜茶 Logo、不临摹 yomsweethome 或其他任何具体画面/构图/角色。只应用风格规则。
- **不脱离体系**：不引入高饱和撞色、不做写实光影、不用复杂渐变、不堆元素。
- 风格是默认值而非束缚：主题的"内容"由用户决定，风格永远服从本规范。
